/**
 * Economy module. The legacy decentralized-compute self-funding manager was
 * removed with the 0G compute backend (Lyra uses an API-key LLM). Reserved
 * for Sui-native economy primitives (treasury limits, spend caps, etc.).
 */
export {}
